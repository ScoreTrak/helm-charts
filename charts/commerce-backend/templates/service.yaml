apiVersion: v1
kind: Service
metadata:
  name: {{ include "commerce-backend.fullname" . }}
  labels:
    {{- include "commerce-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commerce-backend.selectorLabels" . | nindent 4 }}
