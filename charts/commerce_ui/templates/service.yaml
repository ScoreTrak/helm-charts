apiVersion: v1
kind: Service
metadata:
  name: {{ include "commerce_ui.fullname" . }}
  labels:
    {{- include "commerce_ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commerce_ui.selectorLabels" . | nindent 4 }}
