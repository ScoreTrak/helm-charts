apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commerce_ui.fullname" . }}-test-connection"
  labels:
    {{- include "commerce_ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commerce_ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
