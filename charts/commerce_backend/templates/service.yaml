apiVersion: v1
kind: Service
metadata:
  name: {{ include "commerce_backend.fullname" . }}
  labels:
    {{- include "commerce_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commerce_backend.selectorLabels" . | nindent 4 }}
