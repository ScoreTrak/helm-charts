apiVersion: v1
kind: Service
metadata:
  name: {{ include "commercebackend.fullname" . }}
  labels:
    {{- include "commercebackend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commercebackend.selectorLabels" . | nindent 4 }}
