apiVersion: v1
kind: Service
metadata:
  name: {{ include "commerce-ui.fullname" . }}
  labels:
    {{- include "commerce-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commerce-ui.selectorLabels" . | nindent 4 }}
